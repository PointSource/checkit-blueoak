items:
  properties:
    user:
      properties:
        email: {description: the Google email associated with this user, type: string}
        name:
            properties:
              first: {description: the user's first name from Google, type: string}
              last: {description: the user's last name from Google, type: string}
            type: object
      type: object
  type: object
type: array
